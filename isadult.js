let age = 1;

function isAdult (age) {
  if (age >= 18) {
  return true;
  } else {
  return false;
  }
}
console.log(isAdult(age));

//let age = 1;
//function isAdult (age) {return age >= 18;}
//console.log(isAdult(age));
